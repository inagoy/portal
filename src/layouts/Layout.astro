---
import { getNavBarLinks, identity } from "../config";
import { normalizeLang, type Lang } from "../i18n";
import LanguageSelector from "../components/LanguageSelector.astro";
import type { SEOInfo } from "../types/config";

interface Props {
  seo: SEOInfo;
  lang?: Lang;
}

const { seo } = Astro.props;
const url = Astro.url;
const currentUrl = url.pathname.endsWith('/') ? url.pathname.slice(0, -1) : url.pathname;
const paramLang = url.searchParams.get('lang');
const pageLang: Lang = normalizeLang(paramLang ?? (Astro.props.lang as string | undefined));
const navBarLinks = getNavBarLinks(pageLang);
function withLangPath(u: URL, lang: string): string {
  const rest = u.pathname.replace(/^\/(en|es)/, '');
  const path = `/${lang}${rest}`;
  return path;
}
const esHref = withLangPath(url, 'es');
const enHref = withLangPath(url, 'en');

---

<style is:global>
  @font-face {
    font-family: "Inter";
    src: url("/fonts/InterVariable.woff2") format("woff2");
    font-weight: 100 900;
    font-display: swap;
  }

  :root {
    --bg: #251106;
    --text: #f7ecde;
    --muted: rgba(247, 236, 222, 0.6);
    --line: rgba(255, 205, 150, 0.16);
    --soft: rgba(255, 164, 91, 0.08);
  }

  html {
    background-color: var(--bg);
    color: var(--text);
    font-family: "Inter", system-ui, sans-serif;
    font-size: 15px;
    overflow-y: scroll;
    -webkit-text-size-adjust: 100%;
    text-size-adjust: 100%;
  }

  * {
    box-sizing: border-box;
    color: inherit;
    margin: 0;
    padding: 0;
    -webkit-font-smoothing: antialiased;
  }

  body {
    position: relative;
    isolation: isolate;
    min-height: 100vh;
    padding-right: calc(100vw - 100%);
    text-transform: lowercase;
  }

  body::before {
    content: "";
    position: fixed;
    inset: 0;
    z-index: -1;
    background-image: url("/textures/velvet.jpg");
    background-position: center;
    background-repeat: no-repeat;
    background-size: cover;
    mix-blend-mode: soft-light;
    opacity: 0.2;
    pointer-events: none;
  }

  body::after {
    content: "";
    position: fixed;
    inset: 0;
    z-index: -2;
    background:
      radial-gradient(circle at 18% 12%, rgba(210, 108, 34, 0.2), transparent 34%),
      radial-gradient(circle at 82% 18%, rgba(146, 70, 18, 0.18), transparent 30%),
      linear-gradient(180deg, rgba(71, 28, 5, 0.24), rgba(71, 28, 5, 0.08));
    pointer-events: none;
  }

  a {
    text-decoration: none;
  }

  .page {
    max-width: 42rem;
    margin: 0 auto;
    padding: 0 1rem;
  }

  .page-header {
    margin-bottom: 2.25rem;
  }

  .page-title {
    font-size: clamp(1.65rem, 7.2vw, 2.4rem);
    font-weight: 520;
    line-height: 0.98;
  }

  .muted {
    color: var(--muted);
  }

  .text-link {
    color: var(--muted);
    transition: color 180ms ease, text-decoration-color 180ms ease;
  }

  .text-link:hover,
  .text-link:focus-visible {
    color: var(--text);
    text-decoration: line-through;
    outline: none;
  }

  .project-row {
    display: grid;
    gap: 0.85rem;
    border-top: 1px solid var(--line);
    padding: 1.1rem 0;
  }

  .media-project-row {
    grid-template-columns: clamp(4.5rem, 22vw, 5.5rem) minmax(0, 1fr);
    align-items: start;
    padding: 0.95rem 0;
  }

  @media (min-width: 640px) {
    html {
      font-size: 16px;
    }

    .page {
      padding: 0 1.5rem;
    }

    .page-header {
      margin-bottom: 3.5rem;
    }

    .page-title {
      font-size: clamp(2rem, 7vw, 4.25rem);
      line-height: 0.95;
    }

    .project-row {
      grid-template-columns: 8rem 1fr;
      gap: 1rem;
      padding: 1.5rem 0;
    }
  }

  .field {
    width: 100%;
    border: 1px solid var(--line);
    background: transparent;
    padding: 0.75rem 0.9rem;
    color: var(--text);
    outline: none;
  }

  .field::placeholder {
    color: var(--muted);
  }

  .field:focus {
    border-color: var(--text);
  }

  .button {
    border: 1px solid var(--line);
    background: transparent;
    padding: 0.75rem 1rem;
    color: var(--muted);
    transition: color 180ms ease, border-color 180ms ease;
  }

  .button:hover,
  .button:focus-visible {
    border-color: var(--text);
    color: var(--text);
    text-decoration: line-through;
    outline: none;
  }

  @media (min-width: 640px) {
    .field {
      padding: 0.85rem 1rem;
    }

    .button {
      padding: 0.85rem 1.1rem;
    }
  }
</style>

<html lang={pageLang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{seo.title}</title>
    <meta name="description" content={seo.description} />
    <meta property="og:title" content={seo.title} />
    <meta property="og:description" content={seo.description} />
    <meta property="og:image" content={seo.image} />
    
  </head>
  <body>
    <header>
      <div
        class="page flex items-center justify-between py-6 sm:py-8"
      >
        <a href={`/${pageLang}/`} class="text-link text-sm">
          <img
            src={identity.logo}
            alt={"Logo"}
            width="46"
            height="46"
            class="block h-10 w-auto opacity-90 sm:h-[46px]"
          />
        </a>
        <nav class="ml-auto flex items-center gap-3 text-[0.8125rem] sm:gap-5 sm:text-sm">
          <ul class="hidden sm:flex sm:items-center sm:gap-5">
            {
              navBarLinks.map((link) => (
                <li>
                  <a
                    class={`text-link whitespace-nowrap ${currentUrl === link.url ? 'text-[var(--text)] line-through' : ''}`}
                    href={link.url}
                    target={link.external ? "_blank" : "_self"}
                  >
                    {link.title}
                  </a>
                </li>
              ))
            }
          </ul>
          <div class="relative sm:hidden">
            <button
              id="link-menu-button"
              class="text-link inline-flex items-center gap-1 whitespace-nowrap"
              >menu <span id="open-indicator">+</span></button
            >
            <div
              id="link-menu"
              class="pointer-events-none absolute right-0 top-7 z-40 w-40 border border-[var(--line)] bg-[var(--bg)] p-3 opacity-0 transition-opacity duration-200 sm:top-8 sm:w-44"
            >
              <ul>
                {
                  navBarLinks.map((link) => (
                    <li>
                      <a
                        class={`text-link block py-2 whitespace-nowrap ${currentUrl === link.url ? 'text-[var(--text)] line-through' : ''}`}
                        href={link.url}
                        target={link.external ? "_blank" : "_self"}
                      >
                        {link.title}
                      </a>
                    </li>
                  ))
                }
              </ul>
            </div>
          </div>
          <LanguageSelector lang={pageLang} esHref={esHref} enHref={enHref} />
        </nav>
      </div>
    </header>
    <main class="pb-20 sm:pb-28">
      <slot />
    </main>
    <footer></footer>
  </body>
</html>

<script>
  const linkMenuButton = document.getElementById(
    "link-menu-button"
  ) as HTMLButtonElement;
  const linkMenu = document.getElementById("link-menu") as HTMLDivElement;
  const openIndicator = document.getElementById(
    "open-indicator"
  ) as HTMLSpanElement;

  linkMenuButton.addEventListener("click", () => {
    if (linkMenu.classList.contains("opacity-0")) {
      linkMenu.classList.remove("pointer-events-none");
      linkMenu.classList.remove("opacity-0");
      openIndicator.innerText = "-";
    } else {
      linkMenu.classList.add("pointer-events-none");
      linkMenu.classList.add("opacity-0");
      openIndicator.innerText = "+";
    }
  });

  document.addEventListener("click", (event) => {
    const isLinkMenuButtonClicked =
      event.target === linkMenuButton ||
      linkMenuButton.contains(event.target as Node);
    const isLinkMenuClicked =
      event.target === linkMenu || linkMenu.contains(event.target as Node);

    if (!isLinkMenuButtonClicked && !isLinkMenuClicked) {
      linkMenu.classList.add("pointer-events-none");
      linkMenu.classList.add("opacity-0");
      openIndicator.innerText = "+";
    }
  });
</script>
