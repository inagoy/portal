---
import { getNavBarLinks, identity } from "../config";
import { normalizeLang, type Lang } from "../i18n";
import LanguageSelector from "../components/LanguageSelector.astro";
import type { SEOInfo } from "../types/config";

interface Props {
  seo: SEOInfo;
  lang?: Lang;
}

const { seo } = Astro.props;
const pageTitle = seo.title.replace("iñaki goyeneche", "inagoi");
const url = Astro.url;
const currentUrl = url.pathname.endsWith('/') ? url.pathname.slice(0, -1) : url.pathname;
const pageLang: Lang = normalizeLang(Astro.props.lang as string | undefined);
const navBarLinks = getNavBarLinks(pageLang);
function withLangPath(u: URL, lang: string): string {
  const rest = u.pathname.replace(/^\/(en|es)/, '');
  const path = `/${lang}${rest}`;
  return path;
}
const esHref = withLangPath(url, 'es');
const enHref = withLangPath(url, 'en');

---

<style is:global>
  @font-face {
    font-family: "Inter";
    src: url("/fonts/InterVariable.woff2") format("woff2");
    font-weight: 100 900;
    font-display: swap;
  }

  :root {
    --bg: #d87595;
    --paper: #f3efcf;
    --text: #171711;
    --text-strong: #050505;
    --muted: #655f4f;
    --line: #171711;
    --soft: #d8d3b5;
    --accent: #ef3340;
    --cyan: #1ebbb5;
    --yellow: #f0cf3f;
    --media-opacity: 0.9;
    --media-hover-opacity: 1;
  }

  html {
    background-color: var(--bg);
    color: var(--text);
    font-family: "Inter", system-ui, sans-serif;
    font-size: 15px;
    overflow-y: scroll;
    -webkit-text-size-adjust: 100%;
    text-size-adjust: 100%;
  }

  * {
    box-sizing: border-box;
    color: inherit;
    margin: 0;
    padding: 0;
    -webkit-font-smoothing: antialiased;
  }

  body {
    position: relative;
    isolation: isolate;
    min-height: 100vh;
    padding-right: calc(100vw - 100%);
    text-transform: lowercase;
  }

  body::before {
    content: "";
    position: fixed;
    inset: 0;
    z-index: -1;
    background: url("/textures/velvet.jpg") center / cover;
    mix-blend-mode: multiply;
    opacity: 0.1;
    pointer-events: none;
  }

  body::after {
    content: "";
    position: fixed;
    inset: 0;
    z-index: -2;
    background-image: radial-gradient(rgba(23, 23, 17, 0.28) 0.6px, transparent 0.7px);
    background-size: 5px 5px;
    opacity: 0.28;
    pointer-events: none;
  }

  a {
    text-decoration: none;
  }

  a:focus-visible,
  button:focus-visible,
  input:focus-visible,
  textarea:focus-visible,
  summary:focus-visible {
    outline: 2px solid var(--text-strong);
    outline-offset: 3px;
  }

  .site-shell {
    position: relative;
    isolation: isolate;
    width: min(calc(100% - 1rem), 48rem);
    min-height: calc(100vh - 2rem);
    margin: 0.5rem auto 1.5rem;
    overflow: hidden;
    border: 2px solid var(--line);
    background: var(--paper);
    box-shadow: 4px 4px 0 var(--line);
  }

  .site-shell::before {
    content: "";
    position: absolute;
    inset: 0;
    z-index: 0;
    background-image: radial-gradient(rgba(23, 23, 17, 0.22) 0.5px, transparent 0.7px);
    background-size: 4px 4px;
    opacity: 0.26;
    pointer-events: none;
  }

  .site-shell > * {
    position: relative;
    z-index: 1;
  }

  .site-shell > header {
    z-index: auto;
    border-bottom: 2px solid var(--line);
  }

  .mobile-menu-toggle {
    display: flex;
    min-height: 2.75rem;
    cursor: pointer;
    list-style: none;
    align-items: center;
    gap: 0.55rem;
    border: 0;
    background: transparent;
    padding: 0.45rem 0.2rem;
    font-weight: 650;
    text-decoration: underline;
    text-underline-offset: 0.22em;
  }

  .mobile-menu-toggle::-webkit-details-marker {
    display: none;
  }

  .mobile-menu-toggle::after {
    content: "+";
    font-size: 1.1rem;
    line-height: 0;
  }

  .mobile-menu[open] .mobile-menu-toggle::after {
    content: "−";
  }

  .mobile-menu-panel {
    position: absolute;
    z-index: 40;
    top: calc(100% + 0.9rem);
    right: 0;
    width: min(21rem, calc(100vw - 2rem));
    border: 2px solid var(--line);
    background: var(--cyan);
    padding: 0.55rem;
    box-shadow: 4px 4px 0 var(--line);
    animation: mobile-menu-reveal 140ms ease both;
  }

  .mobile-menu-panel li + li {
    margin-top: 0.45rem;
  }

  .mobile-menu-panel a {
    display: flex;
    min-height: 3.25rem;
    align-items: center;
    border: 1.5px solid var(--line);
    padding: 0.75rem 0.9rem;
    color: var(--text-strong);
    font-size: 1rem;
    font-weight: 720;
    text-decoration: none;
    box-shadow: 2px 2px 0 var(--line);
  }

  .mobile-menu-panel li:nth-child(1) a {
    background: var(--paper);
  }

  .mobile-menu-panel li:nth-child(2) a {
    background: var(--yellow);
  }

  .mobile-menu-panel li:nth-child(3) a {
    background: var(--accent);
  }

  .mobile-menu-panel a:hover,
  .mobile-menu-panel a:focus-visible {
    box-shadow: none;
    transform: translate(2px, 2px);
  }

  .mobile-menu-panel a[aria-current="page"]::after {
    content: "•";
    margin-left: auto;
    font-size: 1.25rem;
  }

  .mobile-menu-panel a[aria-current="page"] {
    background: var(--accent);
  }

  @keyframes mobile-menu-reveal {
    from {
      opacity: 0;
      transform: translateY(-0.4rem);
    }
  }

  .page {
    max-width: 42rem;
    margin: 0 auto;
    padding: 0 1rem;
  }

  .page-header {
    margin-bottom: 2.25rem;
    padding-bottom: 1.25rem;
    border-bottom: 2px solid var(--line);
  }

  .page-title {
    display: inline;
    padding: 0.03em 0.14em 0.08em;
    background: var(--accent);
    box-decoration-break: clone;
    -webkit-box-decoration-break: clone;
    font-size: clamp(1.65rem, 7.2vw, 2.4rem);
    font-weight: 800;
    letter-spacing: -0.055em;
    line-height: 1.15;
    text-transform: uppercase;
    box-shadow: inset 0 0 0 2px var(--line);
  }

  .muted {
    color: var(--muted);
  }

  .text-link {
    color: var(--text);
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 0.22em;
    transition: background-color 140ms ease, color 140ms ease;
  }

  .text-link:hover,
  .text-link:focus-visible,
  .text-link[aria-current] {
    background: var(--accent);
    color: var(--text-strong);
    text-decoration: none;
  }

  .social-link {
    display: inline-block;
    border: 1.5px solid var(--line);
    background: var(--paper);
    padding: 0.32rem 0.52rem 0.38rem;
    font-weight: 650;
    line-height: 1;
    text-decoration: none;
    box-shadow: 2px 2px 0 var(--line);
    transition: background-color 140ms ease, box-shadow 140ms ease, transform 140ms ease;
  }

  .social-link:hover,
  .social-link:focus-visible {
    background: var(--yellow);
    box-shadow: none;
    transform: translate(2px, 2px);
  }

  .home-links {
    display: grid;
    gap: 0.75rem;
  }

  .primary-link {
    display: flex;
    min-height: 3.75rem;
    align-items: center;
    justify-content: space-between;
    border: 2px solid var(--line);
    background: var(--cyan);
    padding: 0.85rem 1rem;
    color: var(--text-strong);
    font-weight: 760;
    text-decoration: none;
    box-shadow: 3px 3px 0 var(--line);
    transition: box-shadow 140ms ease, transform 140ms ease;
  }

  .home-links .primary-link:nth-child(even) {
    background: var(--yellow);
  }

  .home-links .primary-link:nth-child(3) {
    background: var(--accent);
  }

  .primary-link:hover,
  .primary-link:focus-visible {
    box-shadow: none;
    transform: translate(3px, 3px);
  }

  .project-row img,
  .music-card-cover img {
    filter: saturate(0.78) contrast(1.06);
    opacity: var(--media-opacity);
    transition: filter 180ms ease, opacity 180ms ease, transform 180ms ease;
  }

  .project-row:hover img,
  .project-row:focus-visible img,
  .music-card:hover .music-card-cover img,
  .music-card:focus-within .music-card-cover img {
    filter: saturate(1) contrast(1.03);
    opacity: var(--media-hover-opacity);
  }

  .project-row {
    display: grid;
    gap: 0.85rem;
    border-top: 2px solid var(--line);
    padding: 1.1rem 0;
  }

  .project-row > :first-child,
  .music-card-cover {
    border: 1.5px solid var(--line);
    border-radius: 0 !important;
  }

  .music-artist-tag {
    border-color: rgba(23, 23, 17, 0.38) !important;
    background: rgba(23, 23, 17, 0.08) !important;
    color: rgba(23, 23, 17, 0.8) !important;
  }

  .kamehouse-list > .music-card-compact::before {
    background: var(--accent) !important;
  }

  .kamehouse-label {
    color: var(--text) !important;
  }

  .invalid-feedback,
  .empty-feedback {
    color: #97152e !important;
  }

  .media-project-row {
    grid-template-columns: clamp(4.5rem, 22vw, 5.5rem) minmax(0, 1fr);
    align-items: start;
    padding: 0.95rem 0;
  }

  @media (min-width: 640px) {
    html {
      font-size: 16px;
    }

    .page {
      padding: 0 1.5rem;
    }

    .page-header {
      margin-bottom: 3.5rem;
    }

    .page-title {
      font-size: clamp(2rem, 7vw, 3.6rem);
    }

    .project-row {
      grid-template-columns: 8rem 1fr;
      gap: 1rem;
      padding: 1.5rem 0;
    }
  }

  .field {
    width: 100%;
    border: 2px solid var(--line);
    border-radius: 0;
    background: rgba(255, 255, 255, 0.24);
    padding: 0.75rem 0.9rem;
    color: var(--text);
    outline: none;
  }

  .field::placeholder {
    color: var(--muted);
  }

  .field:focus {
    background: rgba(255, 255, 255, 0.48);
  }

  .button {
    border: 2px solid var(--line);
    background: var(--yellow);
    padding: 0.75rem 1rem;
    color: var(--text-strong);
    font-weight: 700;
    box-shadow: 3px 3px 0 var(--line);
    transition: background-color 140ms ease, box-shadow 140ms ease, transform 140ms ease;
  }

  .button:hover,
  .button:focus-visible {
    background: var(--accent);
    box-shadow: none;
    color: var(--text-strong);
    transform: translate(3px, 3px);
  }

  @media (min-width: 640px) {
    .field {
      padding: 0.85rem 1rem;
    }

    .button {
      padding: 0.85rem 1.1rem;
    }
  }

  @media (min-width: 640px) {
    .site-shell {
      width: min(calc(100% - 3rem), 48rem);
      margin-top: 1.5rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .text-link,
    .social-link,
    .primary-link,
    .project-row img,
    .music-card-cover img,
    .button,
    .mobile-menu-toggle,
    .mobile-menu-panel,
    .mobile-menu-panel a {
      transition: none;
      animation: none;
    }
  }
</style>

<html lang={pageLang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
    <meta name="description" content={seo.description} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={seo.description} />
    <meta property="og:image" content={seo.image} />
    
  </head>
  <body>
    <div class="site-shell">
      <header>
        <div
          class="page flex items-center justify-between py-6 sm:py-8"
        >
          <a href={`/${pageLang}/`} class="text-link text-sm">
            <img
              src={identity.logo}
              alt={"Logo"}
              width="46"
              height="46"
              class="block h-10 w-auto opacity-[0.82] brightness-0 transition-opacity duration-300 hover:opacity-100 sm:h-[46px]"
            />
          </a>
          <nav class="relative ml-auto flex items-center gap-3 text-[0.8125rem] sm:gap-5 sm:text-sm">
            <ul class="hidden sm:flex sm:items-center sm:gap-5">
              {
                navBarLinks.map((link) => (
                  <li>
                    <a
                      class="text-link whitespace-nowrap"
                      href={link.url}
                      target={link.external ? "_blank" : "_self"}
                      aria-current={currentUrl === link.url ? "page" : undefined}
                    >
                      {link.title}
                    </a>
                  </li>
                ))
              }
            </ul>
            <details id="mobile-menu" class="mobile-menu static sm:hidden">
              <summary class="mobile-menu-toggle">menu</summary>
              <div class="mobile-menu-panel">
                <ul>
                  {
                    navBarLinks.map((link) => (
                      <li>
                        <a
                          class="text-link block whitespace-nowrap py-2"
                          href={link.url}
                          target={link.external ? "_blank" : "_self"}
                          aria-current={currentUrl === link.url ? "page" : undefined}
                        >
                          {link.title}
                        </a>
                      </li>
                    ))
                  }
                </ul>
              </div>
            </details>
            <LanguageSelector lang={pageLang} esHref={esHref} enHref={enHref} />
          </nav>
        </div>
      </header>
      <main class="pb-20 sm:pb-28">
        <slot />
      </main>
      <footer></footer>
    </div>
  </body>
</html>

<script>
  const mobileMenu = document.getElementById("mobile-menu") as HTMLDetailsElement | null;

  document.addEventListener("click", (event) => {
    if (mobileMenu?.open && event.target instanceof Node && !mobileMenu.contains(event.target)) {
      mobileMenu.open = false;
    }
  });

  document.addEventListener("keydown", (event) => {
    if (event.key === "Escape" && mobileMenu?.open) {
      mobileMenu.open = false;
    }
  });
</script>
